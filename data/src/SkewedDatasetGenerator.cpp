#include "SkewedDatasetGenerator.h"

namespace mdsearch
{

	PointList SkewedDatasetGenerator::generate(unsigned int numDimensions,
		const Point& min, const Point& max, Real skewSeverity,
		SkewDirection skewDirection) const
	{
		return PointList();
	}

}